apiVersion: v1
kind: Service
metadata:
  name: webapp
  namespace: apps
  labels:
    app: webapp
spec:
  clusterIP: None
  selector:
    app: webapp
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      # /metrics is on the same port; ServiceMonitor uses port name "http"
