apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: classifier
  namespace: monitoring
  labels:
    release: kube-prometheus-stack
spec:
  namespaceSelector:
    matchNames:
      - apps
  selector:
    matchLabels:
      app: apache
  podMetricsEndpoints:
    - port: metrics
      path: /metrics
      interval: 15s
